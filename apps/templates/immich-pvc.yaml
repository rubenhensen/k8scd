apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: immich-claim
  name: immich-claim
  namespace: immich
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 400Gi
