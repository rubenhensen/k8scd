apiVersion: v1
kind: Service
metadata:
  name: ha-svc
spec:
  ports:
  - name: app
    port: 80
    protocol: TCP
    targetPort: 8123
  selector:
    app: ha-endpoint
  ipFamilies:
  - IPv4
  clusterIP: None
  type: ClusterIP