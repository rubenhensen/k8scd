apiVersion: v1
kind: Service
metadata:
  name: ha-svc
spec:
  selector:
    app.kubernetes.io/name: app
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8123
  externalIPs:
    - 192.168.1.202