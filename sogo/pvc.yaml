apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: sogo-data
  namespace: sogo
spec:
  accessModes:
  - ReadWriteOnce
  storageClassName: longhorn
  resources:
    requests:
      storage: 5Gi
