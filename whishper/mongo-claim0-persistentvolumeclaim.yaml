apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: mongo-claim10
  name: mongo-claim10
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
