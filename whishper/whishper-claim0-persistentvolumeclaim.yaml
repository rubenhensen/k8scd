apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: whishper-claim0
  name: whishper-claim0
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
