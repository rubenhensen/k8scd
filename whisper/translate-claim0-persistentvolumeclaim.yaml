apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: translate-claim0
  name: translate-claim0
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
